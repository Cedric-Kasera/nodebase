import type { NodeExecutor } from "@/features/executions/types";

// TODO: Implement Google Form trigger via custom backend webhook
export const googleFormTriggerExecutor: NodeExecutor = async ({ context }) => context;
